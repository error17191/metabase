# Copy to .env and update values

# Public URL that users will access (https scheme recommended once SSL is set up)
MB_SITE_URL=https://metabase.example.com

# Application database (Postgres) credentials for Metabase internal metadata
MB_DB_DBNAME=metabase
MB_DB_USER=metabase
MB_DB_PASS=change_this_strong_password

# Optional: auto-provision initial admin (uncomment in docker-compose.yml as well if you use these)
# MB_ADMIN_EMAIL=admin@example.com
# MB_ADMIN_FIRST_NAME=Admin
# MB_ADMIN_LAST_NAME=User
# MB_ADMIN_PASSWORD=change_this_admin_password
